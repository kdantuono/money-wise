      - name: ⏱️ Log job duration
        run: |
          echo "Job ${{ github.job }} started at $(date -u +%s)" >> workflow-benchmark.log
          # ...existing code...

# 🛡️ Master Branch Protection & Production Deploy
# Purpose: Protects the main branch and manages production deployment with ultra-strict validation.
# Dependencies: Jobs are executed sequentially to ensure complete validation before deployment.
# Logic: Each step verifies a critical property (coverage, type-check, lint, security, performance), with resource monitoring and failure notifications.

on:
  pull_request:
    branches: [ main ]
    types: [ opened, synchronize, reopened ]

env:
  NODE_VERSION: '18'
  PRODUCTION_URL: 'https://app.moneywise.com'

jobs:
  # ================================
  # ULTRA-STRICT PRODUCTION VALIDATION
  # ================================
  production-readiness-check:
    name: 🎯 Production Readiness Validation
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: 📦 Install Dependencies
        run: |
          npm ci
          cd packages/types && npm run build

      - name: 🔍 Verify Develop Branch Integration
        run: |
          # Ensure this PR comes from develop branch
          if [[ "${{ github.head_ref }}" != "develop" ]]; then
            echo "❌ ERROR: Pull requests to main must come from develop branch only"
            exit 1
          fi
          echo "✅ PR correctly originates from develop branch"

      - name: 📊 Quality Gate Validation
        run: |
          echo "🔍 Running comprehensive quality validation..."

          # Code coverage must be >= 85% for production
          COVERAGE=$(npm run test:coverage --silent | grep "All files" | awk '{print $10}' | sed 's/%//')
          if [ "${COVERAGE%.*}" -lt 85 ]; then
            echo "❌ Code coverage $COVERAGE% below required 85%"
            exit 1
          fi
          echo "✅ Code coverage: $COVERAGE%"

          # Zero TypeScript errors
          npm run type-check
          echo "✅ TypeScript validation passed"

          # Zero linting errors
          npm run lint
          echo "✅ Linting validation passed"

      - name: 🛡️ Security Validation
        run: |
          echo "🛡️ Running production security checks..."

          # High-severity vulnerability check
          npm audit --audit-level high
          echo "✅ No high-severity vulnerabilities"

          # Secret scanning (secure)
          secret_count=$(grep -r "password\|secret\|key" --include="*.ts" --include="*.js" . | grep -v node_modules | grep -v test | wc -l)
          if [ "$secret_count" -gt 0 ]; then
            echo "❌ $secret_count potential secrets detected in code"
            exit 1
          fi
          echo "✅ No hardcoded secrets detected"

        - name: 🔒 Secrets Rotation Validation
          run: |
            if [ -f .env ]; then
              LAST_MOD=$(date -r .env +%s)
              NOW=$(date +%s)
              DIFF=$(( (NOW - LAST_MOD) / 86400 ))
              if [ "$DIFF" -gt 90 ]; then
                echo "❌ .env file is older than 90 days. Please rotate secrets."
                exit 1
              fi
              echo "✅ .env file secrets are recently rotated."
            else
              echo "⚠️ .env file not found. Skipping secrets rotation check."
            fi
          - name: 🐳 Build Docker Image (Backend)
            run: |
              docker build -t moneywise-backend:latest -f apps/backend/Dockerfile .
          - name: 🐳 Docker Image Vulnerability Scan
            uses: aquasecurity/trivy-action@v0.10.0
            with:
              image-ref: 'moneywise-backend:latest'
              format: 'table'
              exit-code: '1'
              ignore-unfixed: true
      - name: �️ Performance Validation
        run: |
          echo "⚡ Running production performance validation..."
          npm run build &
          wait
          npm run budget:check
          echo "✅ Performance budgets met"
      - name: 🖥️ Monitor runner resources
        run: |
          echo "CPU/RAM usage:" && top -b -n1 | head -20
          echo "Disk usage:" && df -h
      - name: 🚨 Notify on failure
        if: failure()
        run: |
          echo "Workflow failed!"

          # Run E2E tests against production build
          npm run test:e2e:prod

          # Cleanup
          docker-compose -f docker-compose.prod.yml down
          echo "✅ Production E2E tests passed"

  # ================================
  # COMPLIANCE & DOCUMENTATION CHECK
  # ================================
  compliance-validation:
    name: 📋 Compliance & Documentation
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: 📚 Documentation Validation
        run: |
          echo "📚 Validating documentation completeness..."

          # Check for required documentation files
          required_docs=("README.md" "CHANGELOG.md" "AGENT_ORCHESTRATION_WORKFLOW.md")
          for doc in "${required_docs[@]}"; do
            if [ ! -f "$doc" ]; then
              echo "❌ Missing required documentation: $doc"
              exit 1
            fi
          done
          echo "✅ All required documentation present"

      - name: 🏷️ Version Tag Validation
        run: |
          echo "🏷️ Validating semantic versioning..."

          # Check if version has been updated
          current_version=$(cat package.json | jq -r '.version')
          echo "Current version: $current_version"

          # Validate semantic version format
          if [[ ! $current_version =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "❌ Invalid semantic version format: $current_version"
            exit 1
          fi
          echo "✅ Valid semantic version format"

      - name: 📝 Changelog Validation
        run: |
          echo "📝 Validating changelog updates..."

          # Check if CHANGELOG.md has been updated
          if ! git diff HEAD~1 --name-only | grep -q "CHANGELOG.md"; then
            echo "❌ CHANGELOG.md not updated for this release"
            exit 1
          fi
          echo "✅ Changelog updated"

  # ================================
  # MANUAL APPROVAL GATE
  # ================================
  manual-approval:
    name: 👥 Technical Lead Approval
    runs-on: ubuntu-latest
    needs: [production-readiness-check, compliance-validation]
    environment: production-approval

    steps:
      - name: ⏳ Waiting for Manual Approval
        run: |
          echo "⏳ Waiting for technical lead approval..."
          echo "This job requires manual approval to proceed with production deployment"

  # ================================
  # PRODUCTION DEPLOYMENT
  # ================================
  production-deployment:
    name: 🚀 Production Deployment
    runs-on: ubuntu-latest
    needs: manual-approval
    environment: production

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: 🏗️ Production Build
        run: |
          npm ci
          cd packages/types && npm run build
          npm run build
        env:
          NODE_ENV: production

      - name: 🐳 Deploy to Production
        run: |
          echo "🚀 Deploying to production..."
          # Add production deployment logic here
          # Could use AWS ECS, Kubernetes, Docker Swarm, etc.

      - name: 🔍 Production Health Check
        run: |
          echo "🔍 Running production health checks..."

          # Wait for deployment to complete
          sleep 30

          # Health check
          curl -f ${{ env.PRODUCTION_URL }}/health

          # Basic functionality test
          curl -f ${{ env.PRODUCTION_URL }}/api/status

          echo "✅ Production deployment successful"

      - name: 📊 Post-Deployment Monitoring
        run: |
          echo "📊 Starting post-deployment monitoring..."

          # Monitor for errors in first 5 minutes
          for i in {1..5}; do
            sleep 60
            curl -f ${{ env.PRODUCTION_URL }}/health
            echo "Health check $i/5 passed"
          done

          echo "✅ Post-deployment monitoring successful"

      - name: 🏷️ Create Release Tag
        run: |
          version=$(cat package.json | jq -r '.version')
          git tag -a "v$version" -m "Production release v$version"
          git push origin "v$version"
          echo "✅ Release tag v$version created"

  # ================================
  # ROLLBACK CAPABILITY
  # ================================
  rollback-preparation:
    name: 🔄 Rollback Preparation
    runs-on: ubuntu-latest
    needs: production-deployment
    if: failure()

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: 🔄 Prepare Rollback
        run: |
          echo "🔄 Preparing rollback to previous stable version..."

          # Get previous stable tag
          previous_tag=$(git tag | grep -E "^v[0-9]+\.[0-9]+\.[0-9]+$" | sort -V | tail -2 | head -1)
          echo "Previous stable version: $previous_tag"

          # Create rollback script
          cat > rollback.sh << EOF
          #!/bin/bash
          echo "Rolling back to $previous_tag"
          git checkout $previous_tag
          # Add rollback deployment commands here
          EOF

          chmod +x rollback.sh
          echo "✅ Rollback script prepared"

  # ================================
  # NOTIFICATIONS & REPORTING
  # ================================
  production-notifications:
    name: 📢 Production Notifications
    runs-on: ubuntu-latest
    needs: production-deployment
    if: always()

    steps:
      - name: 📊 Deployment Report
        run: |
          if [ "${{ needs.production-deployment.result }}" == "success" ]; then
            echo "✅ Production deployment successful"
            status="success"
            color="good"
          else
            echo "❌ Production deployment failed"
            status="failed"
            color="danger"
          fi


      - name: 📧 Email Notification
        if: failure()
        run: |
          echo "📧 Sending failure notification to technical leads..."
          # Add email notification logic here