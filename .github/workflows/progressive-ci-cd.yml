# 🚀 Progressive CI/CD Pipeline
# Evolutionary workflow: adapts automatically from Greenfield → MVP → MMP
# Zero maintenance, zero breaking changes, infinite scalability

name: Progressive CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:
    inputs:
      force_full_pipeline:
        description: 'Force run all pipeline stages'
        required: false
        default: false
        type: boolean

# Global environment variables
env:
  NODE_VERSION: '18'
  FORCE_COLORS: '1'

# Cancel previous runs on new commits
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ===================================================================
  # 🌱 FOUNDATION: Always active - Greenfield ready
  # ===================================================================

  foundation:
    name: 🌱 Foundation Health Check
    runs-on: ubuntu-latest

    outputs:
      has_package_json: ${{ steps.detect.outputs.has_package_json }}
      has_source_code: ${{ steps.detect.outputs.has_source_code }}
      has_tests: ${{ steps.detect.outputs.has_tests }}
      has_apps: ${{ steps.detect.outputs.has_apps }}
      has_docker: ${{ steps.detect.outputs.has_docker }}
      project_stage: ${{ steps.detect.outputs.project_stage }}

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🔍 Detect Project Stage & Components
        id: detect
        run: |
          echo "🔍 Analyzing project structure..."

          # Detect key files/directories
          HAS_PACKAGE_JSON=$([[ -f "package.json" ]] && echo "true" || echo "false")
          HAS_SOURCE_CODE=$([[ -n "$(find . -name "*.ts" -o -name "*.js" -o -name "*.tsx" -o -name "*.jsx" | head -1)" ]] && echo "true" || echo "false")
          HAS_TESTS=$([[ -n "$(find . -name "*.test.*" -o -name "*.spec.*" | head -1)" ]] && echo "true" || echo "false")
          HAS_APPS=$([[ -d "apps" && -n "$(ls -A apps 2>/dev/null)" ]] && echo "true" || echo "false")
          HAS_DOCKER=$([[ -f "docker-compose.yml" || -f "docker-compose.dev.yml" || -f "Dockerfile" ]] && echo "true" || echo "false")

          # Determine project stage
          if [[ "$HAS_SOURCE_CODE" == "true" && "$HAS_APPS" == "true" ]]; then
            STAGE="MMP"
          elif [[ "$HAS_PACKAGE_JSON" == "true" || "$HAS_SOURCE_CODE" == "true" ]]; then
            STAGE="MVP"
          else
            STAGE="GREENFIELD"
          fi

          # Set outputs
          echo "has_package_json=$HAS_PACKAGE_JSON" >> $GITHUB_OUTPUT
          echo "has_source_code=$HAS_SOURCE_CODE" >> $GITHUB_OUTPUT
          echo "has_tests=$HAS_TESTS" >> $GITHUB_OUTPUT
          echo "has_apps=$HAS_APPS" >> $GITHUB_OUTPUT
          echo "has_docker=$HAS_DOCKER" >> $GITHUB_OUTPUT
          echo "project_stage=$STAGE" >> $GITHUB_OUTPUT

          # Display detection results
          echo "📊 PROJECT ANALYSIS RESULTS:"
          echo "🎯 Stage: $STAGE"
          echo "📦 Package.json: $HAS_PACKAGE_JSON"
          echo "💻 Source Code: $HAS_SOURCE_CODE"
          echo "🧪 Tests: $HAS_TESTS"
          echo "📁 Apps: $HAS_APPS"
          echo "🐳 Docker: $HAS_DOCKER"

      - name: 📋 Repository Health Check
        run: |
          echo "🔍 Repository Health Validation..."

          # Required files check
          HEALTH_SCORE=0

          if [[ -f "CLAUDE.md" ]]; then
            echo "✅ CLAUDE.md found"
            HEALTH_SCORE=$((HEALTH_SCORE + 1))
          else
            echo "❌ CLAUDE.md missing"
          fi

          if [[ -d ".claude" ]]; then
            echo "✅ .claude directory found"
            HEALTH_SCORE=$((HEALTH_SCORE + 1))
          else
            echo "❌ .claude directory missing"
          fi

          if [[ -d "docs" && -n "$(ls -A docs 2>/dev/null)" ]]; then
            echo "✅ Documentation directory found"
            HEALTH_SCORE=$((HEALTH_SCORE + 1))
          else
            echo "⚠️ Documentation directory empty or missing"
          fi

          echo "📊 Repository Health Score: $HEALTH_SCORE/3"

          if [[ $HEALTH_SCORE -lt 2 ]]; then
            echo "⚠️ Repository health below threshold"
            exit 1
          fi

          echo "✅ Repository health check passed"

      - name: 🔒 Security Baseline Scan
        run: |
          echo "🔒 Running security baseline scan..."

          # Check for exposed secrets patterns
          echo "🔍 Scanning for secrets..."

          SECRET_FOUND=false

          # AWS Keys
          if grep -r "AKIA[0-9A-Z]{16}" . --exclude-dir=.git 2>/dev/null; then
            echo "❌ AWS Access Key detected!"
            SECRET_FOUND=true
          fi

          # Stripe Live Keys
          if grep -r "sk_live_[0-9a-zA-Z]{24}" . --exclude-dir=.git 2>/dev/null; then
            echo "❌ Stripe Live Key detected!"
            SECRET_FOUND=true
          fi

          # Generic API keys
          if grep -r "api[_-]key[\"']*\s*[:=]\s*[\"'][^\"']{20,}" . --exclude-dir=.git 2>/dev/null; then
            echo "❌ Potential API key detected!"
            SECRET_FOUND=true
          fi

          if [[ "$SECRET_FOUND" == "true" ]]; then
            echo "🚨 Security scan FAILED - secrets detected"
            exit 1
          fi

          echo "✅ Security baseline scan passed"

      - name: 📝 Configuration Validation
        run: |
          echo "📝 Validating configuration files..."

          # Validate existing configs
          if [[ -f ".prettierrc.json" ]]; then
            echo "✅ Prettier config valid"
            if ! jq empty .prettierrc.json 2>/dev/null; then
              echo "❌ .prettierrc.json has invalid JSON"
              exit 1
            fi
          fi

          if [[ -f ".nvmrc" ]]; then
            echo "✅ Node version specified: $(cat .nvmrc)"
          fi

          if [[ -f "docker-compose.dev.yml" ]]; then
            echo "✅ Docker Compose development config found"
          fi

          echo "✅ Configuration validation passed"

  # ===================================================================
  # 📦 DEVELOPMENT: Conditional - Active when package.json exists
  # ===================================================================

  development:
    name: 📦 Development Pipeline
    runs-on: ubuntu-latest
    needs: foundation
    if: needs.foundation.outputs.has_package_json == 'true' || github.event.inputs.force_full_pipeline == 'true'

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 📦 Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: '8.15.1'

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: 📥 Install Dependencies
        run: |
          echo "📥 Installing dependencies..."
          pnpm install --frozen-lockfile

      - name: 🔧 TypeScript Compilation Check
        if: hashFiles('**/tsconfig.json') != ''
        run: |
          echo "🔧 Checking TypeScript compilation..."
          npx tsc --noEmit --skipLibCheck

      - name: 📋 ESLint Validation
        if: hashFiles('**/.eslintrc*') != ''
        run: |
          echo "📋 Running ESLint validation..."
          pnpm lint

      - name: 💅 Prettier Format Check
        if: hashFiles('**/.prettierrc*') != ''
        run: |
          echo "💅 Checking code formatting..."
          npx prettier --check .

  # ===================================================================
  # 🧪 TESTING: Conditional - Active when tests exist
  # ===================================================================

  testing:
    name: 🧪 Testing Pipeline
    runs-on: ubuntu-latest
    needs: [foundation, development]
    if: needs.foundation.outputs.has_tests == 'true' || github.event.inputs.force_full_pipeline == 'true'

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 📦 Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: '8.15.1'

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: 📥 Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: 🧪 Unit Tests
        run: |
          echo "🧪 Running unit tests..."
          pnpm test

      - name: 📊 Test Coverage
        run: |
          echo "📊 Generating test coverage..."
          pnpm test:coverage || echo "⚠️ Coverage not configured"

  # ===================================================================
  # 🏗️ BUILD: Conditional - Active when apps exist
  # ===================================================================

  build:
    name: 🏗️ Build Pipeline
    runs-on: ubuntu-latest
    needs: [foundation, development]
    if: needs.foundation.outputs.has_apps == 'true' || github.event.inputs.force_full_pipeline == 'true'

    strategy:
      matrix:
        app: [backend, web, mobile]

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 🔍 Check App Exists
        id: check-app
        run: |
          if [[ -d "apps/${{ matrix.app }}" && -f "apps/${{ matrix.app }}/package.json" ]]; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "✅ Found app: ${{ matrix.app }}"
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "⏭️ Skipping non-existent app: ${{ matrix.app }}"
          fi

      - name: 📦 Setup pnpm
        if: steps.check-app.outputs.exists == 'true'
        uses: pnpm/action-setup@v2
        with:
          version: '8.15.1'

      - name: 📦 Setup Node.js
        if: steps.check-app.outputs.exists == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: 📥 Install Dependencies
        if: steps.check-app.outputs.exists == 'true'
        run: |
          echo "📥 Installing dependencies..."
          pnpm install --frozen-lockfile

      - name: 🏗️ Build Application
        if: steps.check-app.outputs.exists == 'true'
        run: |
          echo "🏗️ Building ${{ matrix.app }}..."
          pnpm build:${{ matrix.app }}

  # ===================================================================
  # 🔒 SECURITY: Progressive - Enhanced when source code exists
  # ===================================================================

  security:
    name: 🔒 Security Pipeline
    runs-on: ubuntu-latest
    needs: foundation

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 🔒 Enhanced Security Scan
        if: needs.foundation.outputs.has_source_code == 'true'
        run: |
          echo "🔒 Running enhanced security scan..."
          echo "⚠️ Enhanced security scanning requires source code"

      - name: 📦 Dependency Audit
        if: needs.foundation.outputs.has_package_json == 'true'
        run: |
          echo "📦 Running dependency security audit..."
          pnpm audit --audit-level high || echo "⚠️ Security vulnerabilities found"

      - name: 🔍 CodeQL Analysis
        if: needs.foundation.outputs.has_source_code == 'true'
        uses: github/codeql-action/init@v3
        with:
          languages: javascript

      - name: 🔍 Perform CodeQL Analysis
        if: needs.foundation.outputs.has_source_code == 'true'
        uses: github/codeql-action/analyze@v3

  # ===================================================================
  # ✅ SUMMARY: Always runs - Provides pipeline status
  # ===================================================================

  summary:
    name: ✅ Pipeline Summary
    runs-on: ubuntu-latest
    needs: [foundation, development, testing, build, security]
    if: always()

    steps:
      - name: 📊 Pipeline Results Summary
        run: |
          echo "📊 Progressive CI/CD Pipeline Complete"
          echo "🎯 Project Stage: ${{ needs.foundation.outputs.project_stage }}"
          echo ""
          echo "📋 PIPELINE RESULTS:"
          echo "🌱 Foundation: ${{ needs.foundation.result }}"
          echo "📦 Development: ${{ needs.development.result || 'skipped' }}"
          echo "🧪 Testing: ${{ needs.testing.result || 'skipped' }}"
          echo "🏗️ Build: ${{ needs.build.result || 'skipped' }}"
          echo "🔒 Security: ${{ needs.security.result }}"
          echo ""

          # Overall status
          if [[ "${{ needs.foundation.result }}" == "success" ]]; then
            echo "✅ Pipeline completed successfully"
            echo "🚀 Ready for ${{ needs.foundation.outputs.project_stage }} development"
          else
            echo "❌ Pipeline failed - check foundation issues"
            exit 1
          fi