# Application Configuration
NEXT_PUBLIC_APP_NAME=MoneyWise
NEXT_PUBLIC_APP_VERSION=0.1.0
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# Sentry Configuration
# IMPORTANT: Use separate Sentry projects for each environment
# See docs/development/sentry-configuration.md for complete setup guide
#
# Client-side DSN (public, safe to expose)
# Development: https://<key>@o<org>.ingest.sentry.io/<dev-project-id>
# Staging: https://<key>@o<org>.ingest.sentry.io/<staging-project-id>
# Production: https://<key>@o<org>.ingest.sentry.io/<prod-project-id>
NEXT_PUBLIC_SENTRY_DSN=

# Environment name (must match backend SENTRY_ENVIRONMENT)
# Development: development
# Staging: staging
# Production: production
NEXT_PUBLIC_SENTRY_ENVIRONMENT=

# Organization and project slugs (for source map upload)
SENTRY_ORG=your-org-slug
SENTRY_PROJECT=moneywise-development  # Change to staging/production as needed

# Release tracking (optional)
# Development: dev-local
# Staging: moneywise@$(git rev-parse --short HEAD)
# Production: moneywise@{version} (match package.json)
SENTRY_RELEASE=
NEXT_PUBLIC_SENTRY_RELEASE=  # Use same value as SENTRY_RELEASE

# Server/Edge runtime environment (should match NEXT_PUBLIC_SENTRY_ENVIRONMENT)
SENTRY_ENVIRONMENT=

# Environment-specific sampling (optional, overrides defaults)
# Development: 1.0 (100%), Staging: 0.5 (50%), Production: 0.1 (10%)
NEXT_PUBLIC_SENTRY_TRACES_SAMPLE_RATE=

# Debug mode (only enable in development)
NEXT_PUBLIC_SENTRY_DEBUG=false

# Analytics Configuration
NEXT_PUBLIC_ANALYTICS_ENABLED=false

# Banking Integration Configuration
# Enable/disable banking integration feature
NEXT_PUBLIC_BANKING_ENABLED=true

# OAuth redirect base URL (e.g., http://localhost:3000 or https://app.moneywise.com)
NEXT_PUBLIC_OAUTH_REDIRECT_BASE=http://localhost:3000

# OAuth callback path (usually /banking/callback)
NEXT_PUBLIC_OAUTH_CALLBACK_PATH=/banking/callback