# MoneyWise Web App Environment Variables

# Development Authentication Bypass (for UI testing only)
# Set these only in development environments
NEXT_PUBLIC_DEV_AUTH_EMAIL=dev@moneywise.com
NEXT_PUBLIC_DEV_AUTH_PASSWORD=your_secure_dev_password

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3002

# Database URL (if needed for direct frontend connections)
# DATABASE_URL=postgresql://username:password@localhost:5432/moneywise_dev

# Other environment-specific variables
# NEXT_PUBLIC_ENVIRONMENT=development