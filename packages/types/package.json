{
  "name": "@money-wise/types",
  "version": "0.6.1",
  "description": "Shared TypeScript type definitions for MoneyWise",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist coverage",
    "typecheck": "tsc --noEmit",
    "test": "echo 'Skip: @money-wise/types is a type-only package (no runtime logic to test)' && exit 0",
    "test:unit": "echo 'Skip: Type definitions validated by tsc, not Jest' && exit 0",
    "test:watch": "echo 'Skip: No tests for type-only package' && exit 0",
    "test:coverage": "echo 'Skip: No coverage for type-only package' && exit 0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.8",
    "jest": "^30.2.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.9.3"
  },
  "files": [
    "dist"
  ]
}