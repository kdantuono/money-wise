{
  "name": "@money-wise/utils",
  "version": "0.5.0",
  "description": "Shared utility functions for MoneyWise",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist coverage",
    "test": "echo 'Skip: @money-wise/utils is empty placeholder - add tests when code is added (see .claude/quality/test-debt.md)' && exit 0",
    "test:unit": "echo 'Skip: No utility functions implemented yet' && exit 0",
    "test:watch": "echo 'Skip: No tests for empty package' && exit 0",
    "test:coverage": "echo 'Skip: No coverage for empty package' && exit 0",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "date-fns": "^2.30.0",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "@types/jest": "^29.5.8",
    "@types/lodash": "^4.14.202",
    "jest": "^30.2.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3"
  },
  "files": [
    "dist"
  ]
}