# =============================================================================
# MoneyWise Monorepo - Environment Variables Template
# =============================================================================
# Copy this file to .env.local and fill in values for your environment.
# For environment-specific configs, use:
#   - .env.development (local development)
#   - .env.staging (staging environment)
#   - .env.production (production environment)
#
# SECURITY WARNING: Never commit .env files with real secrets to git!
# Use: git update-index --skip-worktree .env (to prevent accidental commits)
# =============================================================================

# ─────────────────────────────────────────────────────────────────────────────
# DEVELOPMENT SETUP (copy values from apps/backend/.env.example and apps/web/.env.example)
# ─────────────────────────────────────────────────────────────────────────────

# BACKEND CONFIGURATION (apps/backend/.env)
PORT=3001
APP_NAME=MoneyWise Backend
APP_VERSION=0.1.0
API_PREFIX=api
CORS_ORIGIN=http://localhost:3000

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=password
DB_NAME=moneywise
DB_SCHEMA=public

# JWT Authentication
JWT_ACCESS_SECRET=dev-secret-min-32-chars-required-for-security!!!
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_SECRET=dev-refresh-secret-min-32-chars-required!!!
JWT_REFRESH_EXPIRES_IN=7d

# Redis Cache & Sessions
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# FRONTEND CONFIGURATION (apps/web/.env.local)
NEXT_PUBLIC_APP_NAME=MoneyWise
NEXT_PUBLIC_APP_VERSION=0.1.0
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# ─────────────────────────────────────────────────────────────────────────────
# OPTIONAL SERVICES
# ─────────────────────────────────────────────────────────────────────────────

# Plaid Integration (Banking APIs)
# Get credentials at: https://dashboard.plaid.com
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox

# Sentry Error Tracking (Optional - leave empty to disable)
SENTRY_DSN=

# AWS CloudWatch (Optional - for monitoring)
CLOUDWATCH_ENABLED=false
AWS_REGION=us-east-1

# ─────────────────────────────────────────────────────────────────────────────
# QUICK START GUIDE
# ─────────────────────────────────────────────────────────────────────────────
#
# 1. Copy this file: cp .env.example .env.local
#
# 2. Copy backend env: cp apps/backend/.env.example apps/backend/.env
#
# 3. Copy frontend env: cp apps/web/.env.example apps/web/.env.local
#
# 4. Start Docker services:
#    docker-compose -f docker-compose.dev.yml up -d
#
# 5. Verify environment:
#    ./.claude/scripts/verify-environment.sh
#
# 6. Start development:
#    pnpm dev
#
# ─────────────────────────────────────────────────────────────────────────────
# SECURITY NOTES
# ─────────────────────────────────────────────────────────────────────────────
#
# Development:
#   - Use dummy/test values (as shown above)
#   - Keep .env files out of version control
#
# Production:
#   - Use strong, randomly-generated secrets
#   - Use managed services (AWS RDS, ElastiCache, etc.)
#   - Rotate secrets regularly
#   - Never expose .env files publicly
#   - Use secrets management (Vault, 1Password, GitHub Secrets, etc.)
#
# JWT Secret Generation:
#   openssl rand -hex 32
#
# =============================================================================